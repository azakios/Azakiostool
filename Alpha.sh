read -p 'ENTER YOUR NAME ' userinput1;
#
if [ "${userinput1:-}" = "Azakios" ]
then
	git  clone https://github.com/azakios/AzakiosTeam.git
fi
if [ "${userinput1:-}" = ">3 characters" ]
then
	printf Try to enter Azakios
fi
if [ "${userinput1:-}" = "Anjuman" ]
then
	git  clone https://github.com/azakios/AzakiosTeam.git
fi
if [ "${userinput1:-}" = "Mushahid" ]
then
	git  clone https://github.com/azakios/AzakiosTeam.git
fi
